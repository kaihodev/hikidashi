import type { ArrayLike } from '@/shared/generics';
import ArraySampler from '@/shared/ArraySampler';

/**
 * @todo
 */
export default function sample(arr: ArrayLike, fromIndex: number, toIndex: number): any { ArraySampler.sample(arr, fromIndex, toIndex); }
