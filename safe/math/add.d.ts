import type { NumberLike } from '@/shared/generics';
/**
 * Adds two given NumberLikes.
 *
 * @param x - The augend.
 * @param y - The addend.
 * @returns number - The resulting sum.
 */
declare function add(x?: NumberLike, y?: NumberLike): number;
export default add;
